# Installs OpenVPN. Deploying client profiles and running tunnels is left to
# the caller.

- name: Install OpenVPN
  apt:
    name: "{{ openvpn_packages }}"
    state: present
    update_cache: true
